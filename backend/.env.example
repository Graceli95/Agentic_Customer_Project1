# ============================================================================
# Advanced Customer Service AI - Backend Environment Variables
# ============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to version control
# ============================================================================

# ----------------------------------------------------------------------------
# Phase 2: OpenAI Configuration (REQUIRED)
# ----------------------------------------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
# Used by: backend/agents/simple_agent.py for GPT-4o-mini model
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# ----------------------------------------------------------------------------
# LangSmith Configuration (OPTIONAL - Recommended for Development)
# ----------------------------------------------------------------------------
# LangSmith provides tracing and debugging for LangChain agents
# Sign up at: https://smith.langchain.com/
# View traces at: https://smith.langchain.com/

# Enable tracing (true/false)
LANGSMITH_TRACING=true

# Your LangSmith API key
# Get it from: https://smith.langchain.com/settings
LANGSMITH_API_KEY=lsv2_pt_your-langsmith-api-key-here

# Project name for organizing traces
LANGSMITH_PROJECT=customer-service-phase2

# ----------------------------------------------------------------------------
# AWS Bedrock Configuration (Phase 6 - Future)
# ----------------------------------------------------------------------------
# Currently not used in Phase 2
# Will be needed in Phase 6 for multi-provider LLM support
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1

# ----------------------------------------------------------------------------
# Application Configuration
# ----------------------------------------------------------------------------
# Application environment (development, staging, production)
ENVIRONMENT=development

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Server host and port
HOST=0.0.0.0
PORT=8000

# CORS origins (comma-separated list)
# For development: allow localhost frontend
CORS_ORIGINS=http://localhost:3000

# ----------------------------------------------------------------------------
# Database Configuration (Phase 6 - Future)
# ----------------------------------------------------------------------------
# Not used in Phase 2 (using InMemorySaver for conversation memory)
# Will be needed in production for persistent conversation storage
# DATABASE_URL=postgresql://user:password@localhost:5432/customer_service
# REDIS_URL=redis://localhost:6379/0

# ============================================================================
# Quick Setup Guide
# ============================================================================
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Required for Phase 2:
#    - Add your OPENAI_API_KEY (REQUIRED)
#    - Optionally enable LangSmith tracing for debugging
#
# 3. Start the backend:
#    cd backend
#    source venv/bin/activate  # On Windows: venv\Scripts\activate
#    python main.py
#
# 4. Verify setup:
#    - Backend should start without errors
#    - If OPENAI_API_KEY is missing, you'll see an error at startup
#    - Visit http://localhost:8000/docs to see API documentation
#
# ============================================================================
# Troubleshooting
# ============================================================================
# - "OPENAI_API_KEY must be set": Add your OpenAI API key to .env
# - "Rate limit exceeded": You've hit OpenAI API rate limits (wait or upgrade plan)
# - LangSmith not working: Verify LANGSMITH_API_KEY and LANGSMITH_TRACING=true
#
# For more help, see: backend/README.md
# ============================================================================
